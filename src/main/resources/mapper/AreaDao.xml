<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atom.o2o.dao.AreaDao">
    <select id="queryArea" resultType="com.atom.o2o.entity.Area">
        select * from tb_area order by priority
    </select>
</mapper>